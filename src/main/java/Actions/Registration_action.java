package Actions;
import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedCondition;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;



public class Registration_action {
/*
	 public static void Execute(WebDriver driver){	
		 WebDriverWait wait = new WebDriverWait(driver, 60);
  //wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//input[contains(@class,'return_button')]")));
		 System.out.println("test");		
	     Home_Page.new_registration(driver).click();	 
	 }
	 public static void ExecuteMobile(WebDriver driver){	
		 WebDriverWait wait = new WebDriverWait(driver, 60);
		 
		 wait.until((ExpectedConditions.elementToBeClickable(By.xpath("//div[contains(@class,'next-button')]/input"))));
	//	 wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//div[contains(@class,'next-button')]/input")));
			
	     Home_Page.new_registrationMobile(driver).click();	 
	 }
	 public static void selectPeriod(WebDriver driver){	
		 WebDriverWait wait = new WebDriverWait(driver, 10);
		 wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//input[@value='Получить деньги онлайн']")));
			
	     Home_Page.select_period(driver).click();	 
	 }
	 public static void selectSumma(WebDriver driver){	
		 WebDriverWait wait = new WebDriverWait(driver, 10);
		 wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//input[@value='Получить деньги онлайн']")));
			
	     Home_Page.select_Summa(driver).click();	 
	 }*/
}
